Save the image you attached to this conversation as `bg_full.jpg` in this folder:

static/images/bg_full.jpg

Then reload the site (or refresh the ngrok URL) to see the image used as the full-page background.

Notes:
- For best results, crop/resize the image to a wide aspect ratio (1200x800 or larger).
- The CSS applies `filter: brightness(0.45)` so content remains readable; adjust `brightness` in `static/css/flower.css` if needed.
